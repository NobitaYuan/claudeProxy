# 提供者类型: glm (默认) | 未来支持 kimi 等
PROVIDER_TYPE=glm

# 上游 API 端点（优先读此变量，回退到 GLM_API_BASE）
API_BASE=https://open.bigmodel.cn/api/anthropic

# API Keys（逗号分隔，优先读此变量，回退到 GLM_API_KEYS）
API_KEYS=sk-xxx,sk-yyy,sk-zzz

# 服务端口
PORT=3000

# 管理 API 认证 token
ADMIN_TOKEN=admin-secret-token
